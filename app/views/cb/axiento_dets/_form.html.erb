<%= form_for(@cb_axiento_det) do |f| %>
  <% if @cb_axiento_det.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cb_axiento_det.errors.count, "error") %> prohibited this cb_axiento_det from being saved:</h2>

      <ul>
      <% @cb_axiento_det.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :proyecto_id %><br>
    <%= f.number_field :proyecto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
